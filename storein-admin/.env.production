# Production build: leave VITE_API_BASE_URL empty so http.service.js falls back
# to the relative '/api/v1' path, which is proxied by server.js to the backend.
VITE_API_BASE_URL=
VITE_APP_NAME=Storein Admin
